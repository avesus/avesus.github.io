# Self-hosted Fonts

These `.woff2` files are local copies of the Google Fonts used by the shared
site scripts:

- EB Garamond 400
- Inconsolata 400
- Spectral 200

The font-face CSS lives in `greenforest-fonts.css`, with `font-display: block`
so the shared scripts can reveal the page after the real page fonts are ready.
