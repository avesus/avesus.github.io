# SystemVerilog language-shapes lab

This small simulation distinguishes packed vectors from unpacked arrays,
elaborates four constant-bound parity lanes, and checks the result with an
immediate assertion.

Run:

```sh
make
```

A passing run prints:

```text
PASS: packed words, unpacked memory, loop, and assertion
```
